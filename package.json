{
  "name": "localtube",
  "version": "1.3.0",
  "description": "Chrome extension that gives YouTube a local, logged-out account: subscriptions, a subscriptions-only home feed, playlists, and likes stored in your browser.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
